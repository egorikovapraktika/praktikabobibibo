@startuml
start
:Технолог создаёт новую рецептуру (черновик);
:Добавляет компоненты с процентными долями;
:Сохраняет рецептуру в статусе "draft";

if (Принято решение утвердить?) then (да)
  :Технолог инициирует утверждение;
  :Система проверяет сумму долей компонентов;
  if (Сумма = 100%?) then (нет)
    :Отобразить ошибку "Сумма не равна 100%";
    stop
  else (да)
    :Система ищет другую активную рецептуру для этого продукта;
    if (Есть активная версия?) then (да)
      :Перевести старую версию в архив;
    endif
    :Присвоить рецептуре статус "active";
    :Записать дату утверждения и автора;
    :Создать событие в аудите;
  endif
else (нет)
  :Рецептура остаётся в статусе "draft";
endif
stop
@enduml